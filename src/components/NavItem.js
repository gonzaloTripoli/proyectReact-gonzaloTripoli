const NavItem = (content) => {
    return (
        <a href="default.asp" class="hover:bg-sky-50 rounded-lg border-double border-8 border-transparent" >{content.name}</a>

    )
}
export default NavItem